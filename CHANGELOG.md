# 变更日志 (Changelog)

所有本项目的阶段性功能完成、Bug 修复和安全更新都将记录在此文件中。

本项目遵循 [语义化版本号 (SemVer)](https://semver.org/lang/zh-CN/) 规范。
同时遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式标准。

## [1.4.3] - 2026-08-20

### Added
- **独立全屏：美式 POS 机经典小费压迫屏 (Neta US Tip Culture)**：
  - 行程长按结束后，司机立即将屏幕“旋转递给乘客”，独立弹出原汁原味的美式 POS 触屏小费选择界面（Square / Toast 风格）；包含 18%、20%（默认高亮）、25%、30% 巨型方块小费选项及金额换算，配合 Custom Tip 与底部极小灰字 No Tip（残忍拒绝 🥺）的道德压迫感设计；选择后才正式打印热敏小票。
- **拟物化复古热敏小票结算体验**：
  - 小票内专注展示清晰规范的客运运价明细、路桥费、小费金额与最终合计，保留 1:1 正方形收款码与向右滑动撕纸结单交互。
- **车载防颠簸长按 1.5 秒结束行程**：
  - 结束按钮新增长按 1.5 秒进度遮罩填充防误触机制，避免行车颠簸时一触即停。
- **行车跳表压迫感机械音效 (整元进位触发)**：
  - 计价器车费整数大关每递增 1 元时（例如 16元→17元→18元），精准触发一声清脆机械“嘟”与微震动；小数位分角波动保持安静，彻底消除车开几秒就响一次的狂响干扰。
- **设置面板 iOS 紧凑分组与安全区适配**：
  - 修复设置页顶部被手机灵动岛/状态栏遮挡的问题，严格应用 `safe-area-inset-top` 安全边距；重构为 iOS 紧凑分组卡片，单屏尽览所有配置，彻底消除冗长松散感；新增“恢复官方默认费率 (Reset)”一键复位功能。
- **收款码全自由拖拽平移（Pan）与缩放（Zoom）裁剪**：
  - 裁剪框升级支持手指/鼠标任意方向拖拽移动图片位置与九宫格辅助对齐，轻松将照片角落中的二维码平移至取景框正中并裁出 512×512 高清正方形。

### Fixed
- **修复美式 POS 小费 20% 与 30% 卡片金额预览显示为 0 元的 Bug**：
  - 修复因浮点转字符串（`0.20` 自动转为 `0.2`）导致 DOM 属性选择器无法命中对应卡片的问题，重构为直接遍历小费按钮节点，确保 18%、20%、25%、30% 四档金额实时 100% 正确展示。
- **修复跨夜间时段历史里程被全单追溯重算的漏洞**：
  - 重构为 `calculateSegmentedFare` 严密分段算法，白天与夜间里程各自分区累计，上车时段只决定起步价，彻底消除跨 23:00/05:00 临界点时的全单追溯加价漏洞。
- **修复 iOS Safari Web Audio 自动休眠静音隐患**：
  - 引入音频状态自动唤醒机制（`ensureAudioActive`），在任意用户交互时自动激活 AudioContext。
- **GPS 状态中文化**：
  - 状态栏 GPS 提示全部替换为通俗易懂的中文反馈（如“🛰️ 正在搜星...”、“🛰️ 卫星已连接”、“⚠️ 穿桥跳点已过滤”）。

## [1.4.2] - 2026-07-05

### Added
- 新增 `.gitignore`，忽略 macOS `.DS_Store` 系统文件，降低本地提交混入无关文件的概率。
- 新增 `icon-192.png`、`icon-512.png` 与 `apple-touch-icon.png`，补齐 PWA 安装所需的标准 PNG 图标尺寸，提升 Android 与 iOS 添加到主屏幕时的图标兼容性。
- 新增显式 favicon 声明，复用现有 SVG 图标，避免浏览器自动请求 `/favicon.ico` 产生 404。

### Changed
- 将 `service-worker.js` 缓存版本升级至 `taxi-meter-v1.4.2`，并改为“导航请求网络优先、核心静态资源缓存优先并后台刷新”的策略，使已安装 PWA 在发布后能更快拿到新入口文件，同时保留离线可用能力。
- 重写 README 为客观技术说明，明确项目用途、运行方式和 GPS 里程计费限制，减少误用空间。

### Fixed
- 修复返空里程起征点小于起步里程时产生负数正常里程段的问题，计费公式现在强制 `emptyKm >= baseKm`，避免车费被错误抵扣。
- 修复极端输入值污染账单的问题，费率、附加费、小费和本地缓存恢复现在统一校验有限数字并限制在 `0` 至 `999999` 范围内，避免界面显示 `NaN` 或 `Infinity`。
- 修复 GPS 静止样本不更新时间锚点导致红灯后漏计的问题，静止或轻微漂移时也会刷新采样时间，减少长时间等待后低速起步被误判为未移动的概率。
- 修复结束行程后排队定位回调仍可能修改账单的问题，定位成功和失败回调现在都会先检查行程运行状态，结束时同步清空定时器与定位监听 ID。
- 修复弱信号跳点可能直接累计里程的问题，GPS 样本现在统一校验经纬度、精度、时间递增、单段距离和估算速度，降低一次坏点造成公里级误差的风险。
- 修复重置时未释放屏幕常亮锁的问题，并确保行程中页面切回前台会重新申请常亮锁，减少异常流程后的持续耗电和后台恢复后的熄屏风险。

### Security
- 收款码上传现在仅接受 PNG、JPG、WebP，并压缩到二维码展示所需尺寸后再写入本地缓存；单张压缩后图片限制为 512KB，降低 `localStorage` 配额耗尽和非位图格式注入风险。
- 配置缓存现在只保存费率对象，不再把收款码随 `config` 重复写入；旧配置读取后会静默写回精简结构，单张二维码在本地存储中从最多两份降为一份。
- 旧收款码缓存恢复时同样校验 MIME 类型和 512KB 体积上限，不符合要求的历史缓存会被清理，避免旧数据绕过新上传限制。

## [1.4.1] - 2026-06-10

### Changed
- **LocalStorage 专属前缀命名空间隔离**：将 `taxi_config` 和 `taxi_qr` 分别重构为具有独立前缀的 `retro_taxi_meter_config_v1` 和 `retro_taxi_meter_qr_v1`，降低 GitHub Pages 同源页面之间的本地缓存键名冲突风险。
- **数据平滑自动迁移**：新增了 `migrateLegacyStorage()` 逻辑，老用户在升级后，系统会自动检测并把旧 Key 里的自定义运价和收款码静默转存至新 Key 中，并移除残留旧键，实现无感平滑升级。
- **高频重绘性能优化**：重构了 `updateDisplay()` 中 LED 金额字号自适应缩放（`long-value` 类）的 classList 操作，引入类状态缓存检测，避免每秒无条件增删 class 带来的 CPU 渲染样式重计算（Recalculate Style）性能瓶颈。
- **高架与水泥森林 GPS 测距防漏记优化**：
  - 将无系统瞬时速度反馈时的位置精度阈值从 `40米` 宽限放宽到 `80米`，以适应密集都市核心区及高架桥下的 GPS 信号衰减。
  - 原生速度测速 `speed > 0.2 m/s` 作为第一优先级通道且不限制精度，只要设备感应到物理位移即累加里程，完美杜绝城市大厦反射导致的漏计费现象。

## [1.4.0] - 2026-06-10

### Added
- 在 `script.js` 中引入了全局版本号常量 `VERSION`，规范化运行时版本输出。
- 在 `index.html` 中的费率及附加费数字输入框中新增了原生最小值为 0 的属性限制 (`min="0"` 和 `step="any"`)。

### Fixed
- **修复了重影计费与内存泄漏漏洞**：在 `resetApp()` 重置函数中，新增了注销后台运行的 `state.timerId`（秒级计时器）和 `state.watchId`（GPS 定位监听器）的控制逻辑，彻底消除了后台定时器多重叠加造成的计费加倍及手机发热卡死隐患。
- **引入了非负数值防倒扣保护**：在设置保存 `saveSettings()`、附加费录入 `goToTip()` 以及自定义小费逻辑中，引入了 `Math.max(0, ...)` 非负数转换防护，彻底杜绝了司机或乘客通过输入负数导致总账单出现倒扣扣减的财务逻辑崩溃漏洞。
- 修复了 `index.html` 的窗口标题（`<title>`）版本号滞后问题，更新为与主版本一致的 `v1.4.0`。

### Security
- **修复了本地存储型 XSS 注入漏洞**：在 `script.js` 从本地缓存（`localStorage`）恢复收款码预览区时，弃用了图省事直接拼接 HTML 字符串的 `innerHTML` 接口，改用安全的 `document.createElement` 及 `appendChild` DOM 操作，阻断了恶意收款码 Base64 脚本注入执行的通路。

## [1.3.0] - 2026-05-29

### Added
- **新增了保姆级双向返回流转功能**：
  - 在【输入附加费】页面增加了 `← 返回` 物理按键，支持退回主计价器并保持行程结束但未重置的状态。
  - 在【选择小费】页面增加了并排的 `← 修改附加费` 大按钮，并配备寄存器自动保留上一级输入的附加费。
  - 在【请支付扫码页】左下角增加了 `← 修改小费` 回退功能，允许司机和乘客在最终付款前无忧纠偏。
- **添加了网贷代扣扣款后的回退封锁机制**：一旦乘客通过“打车借呗”借款并自动划扣车资，系统将强制锁定当前账单并隐藏所有“返回修改”按钮，仅保留“再来一单”，防止借款成交后修改原始数据的安全漏洞。
- **PWA 白天模式 LCD 返回按键视觉适配**：为白天模式掌机液晶屏样式添加了专属的 `.btn-back` 浅灰绿拟物风格边框，视觉层级完美统一。

### Fixed
- 重构并完善了 `resetApp()` 彻底大扫除清理机制，重置时强制清空了 `state.lastPos` 与 `state.lastTimestamp` 物理 GPS 定位尾数缓存，彻底封杀了下一单起步时的“信号瞬发漂移跃变”跳字漏洞。
- 修复了大数网贷付款后，再次重置开启新一单时，物理按钮保留金色闪烁警报特效且点击 handler 异常的漏洞，使变异按钮彻底重置打回原形。

## [1.2.0] - 2026-05-29

### Added
- **新增了「打车一键渡劫贷」中国特色小额网贷整活彩蛋**：当订单达到极值封顶 `999999.00` 元时，支付按钮自动变异为金色高频警报呼吸闪烁状态，点击直达血红网贷清算页面，包含大厂金融卡片设计、限时特惠年化 250% 利率（首期前 5 秒免息）、默认勾选霸王协议、以及代扣付清车资后展示 PAID 倾斜安全印章等趣味 Neta。
- **引入了大数溢出防御机制**：
  - 对行程运价及最终账单设定了 `999,999.00` 元极限物理封顶拦截。
  - 添加了 LED 屏幕金额字号的自适应缩放（`.long-value` 及 `.super-long-value` 样式类），根据字符长度动态压榨显示面积，防范大数把数码管撑爆折行。

### Changed
- **扁平化费率参数配置**：彻底删除了“选择城市”的下拉列表，将费率设置扁平化为 5 个直观的数字输入框（默认上海数据作为初始出厂参数），支持司机输入任意超大数值。

## [1.1.0] - 2026-05-28

### Added
- **新增了 PWA 级离线全屏体验**：编写了矢量应用图标 `icon.svg`、`manifest.json`、以及 `service-worker.js` 静态资源缓存，支持 iPhone 全屏灵动岛避让和脱网运行。
- **物理音效与触震反馈**：使用 Web Audio API 在 `script.js` 中纯代码合成咔哒 Click（弹簧微动）及啪嗒 Clack（机械翻牌）音效，并配合 `navigator.vibrate` 实现物理反馈。
- **屏幕常亮控制**：集成 Screen Wake Lock API，防止设备在长途运行或计费时屏幕熄灭中断。
- **昼夜加收费率**：支持在 `23:00 - 次日 05:00` 自动判定夜间加收 30% 费率。
